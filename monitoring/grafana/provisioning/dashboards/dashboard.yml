apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    # Windows bind mounts can intermittently fail frequent directory scans; poll less often.
    updateIntervalSeconds: 300
    options:
      path: /etc/grafana/provisioning/dashboards
